# THIS FILE IS AI GENERATED.
#
# Runtime dependencies for the tppr DigitalOcean Functions deployment.
# Mirrors backend/pyproject.toml minus dev/test deps and the local
# `tppr-paper-extractor` workspace package (which is vendored into the
# function directory instead of pip-installed). Installed remotely on
# DigitalOcean's Linux build fleet via `doctl serverless deploy --remote-build`.
bcrypt>=5.0.0
flask>=3.1.3
flask-cors>=6.0.2
flask-limiter>=4.1.1
flask-sqlalchemy>=3.1.1
flask-swagger-ui>=5.32.5
imohash>=1.1.0
psycopg2-binary>=2.9.12
pydantic>=2.13.3
pyjwt[crypto]>=2.13.0
pyotp>=2.9.0
python-dotenv>=1.2.2
qrcode[pil]>=8.2
sqlmodel>=0.0.38
werkzeug>=3.1.8
