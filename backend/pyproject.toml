[project]
name = "tppr-backend"
version = "0.1.0"
description = "backend for Thribhus Past Paper Repository (tppr)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
]
